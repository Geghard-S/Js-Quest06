/*
**
** QWASAR.IO -- my_each
**
**
** @param {String[]} param_1
**
*/


function my_each(my_each) {
    index = 0;

    while(index < my_each.length){
        console.log(my_each[index])
        index ++;
    }
};